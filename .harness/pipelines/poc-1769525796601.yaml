pipeline:
  name: Build poc
  identifier: Build_poc_1769525801995
  projectIdentifier: appdemo
  orgIdentifier: default
  stages:
    - stage:
        name: Build
        identifier: Build
        type: CI
        spec:
          cloneCodebase: true
          execution:
            steps:
              - step:
                  type: Run
                  name: Echo Welcome Message
                  identifier: Echo_Welcome_Message
                  spec:
                    shell: Sh
                    command: echo "Welcome to Harness CI"
              - step:
                  type: Test
                  name: Test_1
                  identifier: Test_1
                  spec:
                    connectorRef: <+input>
                    image: golang:1.15
                    shell: Sh
                    command: |-
                      go test -v ./...
                      go test -cover ./...
                      go test -v ./pkg/module1 ./pkg/module2
                       go test -race ./...
                    reports:
                      type: JUnit
                      spec:
                        paths:
                          - "*xml"
                    intelligenceMode: true
                  description: unit test
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Cloud
            spec: {}
  properties:
    ci:
      codebase:
        connectorRef: account.github
        repoName: poc
        build: <+input>
